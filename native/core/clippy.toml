# Core receives platform facts through parameters. File, process, network,
# environment, and terminal access belong to native adapters.

disallowed-methods = [
  { path = "std::path::Path::canonicalize", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::Path::metadata", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::Path::symlink_metadata", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::Path::read_dir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::Path::exists", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::Path::try_exists", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::Path::is_file", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::Path::is_dir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::Path::is_symlink", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::read", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::read_to_string", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::write", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::read_dir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::read_link", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::metadata", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::symlink_metadata", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::canonicalize", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::copy", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::rename", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::hard_link", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::remove_file", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::remove_dir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::remove_dir_all", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::create_dir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::create_dir_all", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::set_permissions", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::exists", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::current_dir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::set_current_dir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::current_exe", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::var", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::var_os", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::vars", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::vars_os", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::args", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::args_os", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::temp_dir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::set_var", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::env::remove_var", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::io::stdin", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::io::stdout", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::io::stderr", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::path::absolute", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::thread::spawn", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::thread::sleep", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
]

disallowed-types = [
  { path = "std::fs::File", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::OpenOptions", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::DirBuilder", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::ReadDir", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::fs::DirEntry", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::process::Command", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::process::Child", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::process::Stdio", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::net::TcpStream", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::net::TcpListener", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::net::UdpSocket", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
]

disallowed-macros = [
  { path = "std::print", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::println", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::eprint", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
  { path = "std::eprintln", reason = "Keep platform access outside native/core; pass the required facts as parameters." },
]
