[package]
name = "inferay-native-diff"
version = "0.1.0"
edition = "2021"

[dependencies]
inferay-core = { path = "../core" }
wait-timeout = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
